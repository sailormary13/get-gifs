If using XAMPP, put project folder in XAMPP's htdocs folder.
Navigate to localhost:8080/project4/index.html.
A future consideration would be to have it display fixed_height
  images on desktop and fixed_width images on mobile. Right now it's
  height because it looks way better than width on desktop, but some
  of the widths go slightly off screen on mobile.
